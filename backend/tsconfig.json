{
  "compilerOptions": {
    "target": "ES6",                   // 编译后的 JavaScript 版本
    "module": "commonjs",              // 使用 commonjs 模块
    "strict": true,                    // 启用所有严格类型检查选项
    "esModuleInterop": true,           // 允许使用 ES6 模块
    "skipLibCheck": true,              // 跳过库文件检查
    "forceConsistentCasingInFileNames": true,  // 强制大小写一致的文件名
    "outDir": "./dist",                // 输出目录
    "rootDir": "./src",                // 根目录为 src
    "resolveJsonModule": true          // 支持 JSON 文件的导入
  },
  "include": [
    "src/**/*.ts"  // 确保包含 src 目录下的所有 TypeScript 文件
  ],
  "exclude": [
    "node_modules", // 排除 node_modules 文件夹
    "dist"          // 排除编译输出的文件夹
  ]
}
